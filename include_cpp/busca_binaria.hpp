#pragma once
#include <vector>

int busca_binaria_Padrao(const std::vector<int>& arr, int alvo);

// Funcao 5.1 Primeira versao defeituosa
int busca_binaria(int n);
// API badVersion
bool badVersion(int version);
